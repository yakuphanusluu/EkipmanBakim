<Window x:Class="EkipmanBakimProjesi.BakimTakipEkrani"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Bakım Takip Paneli" Height="700" Width="1150" WindowStartupLocation="CenterScreen" Background="#FFF5F5F5">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <!-- Yıldız (*) ayarı tablonun ekranın geri kalanını doldurmasını sağlar -->
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Bakımı Gelen ve Yaklaşan Ekipmanlar" FontSize="20" FontWeight="Bold" Foreground="#2C3E50" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <!-- ÇÖZÜM: ScrollViewer.VerticalScrollBarVisibility="Auto" eklendi -->
        <DataGrid Grid.Row="1" x:Name="DgBakimTakip" AutoGenerateColumns="False" IsReadOnly="True" 
                  HeadersVisibility="Column" Background="White" RowHeight="35" FontSize="14" AlternatingRowBackground="#F9F9F9"
                  ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Auto">
            <DataGrid.Columns>

                <!-- 1. SÜTUN: Ekipman Adı -->
                <DataGridTextColumn Header="Ekipman Adı" Binding="{Binding EkipmanAdi}" Width="Auto" MinWidth="250">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                            <Setter Property="MaxWidth" Value="400"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="5"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- 2. SÜTUN: Durum -->
                <DataGridTemplateColumn Header="Durum" Width="Auto" MinWidth="130">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="{Binding DurumRengi}" CornerRadius="5" Padding="15,5" Margin="5">
                                <TextBlock Text="{Binding Durum}" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- 3. SÜTUN: Bakıma Kalan Süre -->
                <DataGridTextColumn Header="Bakıma Kalan Süre" Binding="{Binding KalanSureMetin}" Width="Auto" MinWidth="180">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{Binding DurumRengi}"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="5"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- 4. SÜTUN: Son Bakım Tarihi -->
                <DataGridTextColumn Header="Son Bakım Tarihi" Binding="{Binding SonBakim}" Width="Auto" MinWidth="150">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="5"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- 5. SÜTUN: Arıza İşlemleri (Detay ve Çöz Butonları Yan Yana) -->
                <DataGridTemplateColumn Header="Arıza İşlemleri" Width="Auto" MinWidth="180">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Visibility="{Binding ArizaCozGoster}" HorizontalAlignment="Center">
                                <!-- DETAY BUTONU -->
                                <Button Content="🔍 Detay" Background="#64748B" Foreground="White" FontWeight="Bold" 
                                        Padding="10,2" Margin="2" Click="BtnArizaDetay_Click">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="4"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>

                                <!-- ÇÖZ BUTONU -->
                                <Button Content="✔️ Arızayı Çöz" Background="#3B82F6" Foreground="White" FontWeight="Bold" 
                                        Padding="10,2" Margin="2" Click="BtnArizaCoz_Click">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="4"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>